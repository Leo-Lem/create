// Created by <#NAME#> on <#DATE#>.

import SwiftUI

struct AppView: View {
  var body: some View {
    TabView {
      <#TITLE#>View()
    }
  }
}

// MARK: - (PREVIEWS)

#if DEBUG
  struct AppView_Previews: PreviewProvider {
    static var previews: some View {
      AppView()
    }
  }
#endif
